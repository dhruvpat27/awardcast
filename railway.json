{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "python3 -m uvicorn backend.api.main:app --host 0.0.0.0 --port 8000"
  }
}